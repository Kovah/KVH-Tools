<button type="submit" class="inline-block py-2 px-4 border border-green-700 hover:bg-green-700 rounded text-center"
  <%= defined?(value) ? "value=#{value}" : '' %> <%= defined?(name) ? "name=#{name}" : '' %>>
  <%= text %>
</button>
