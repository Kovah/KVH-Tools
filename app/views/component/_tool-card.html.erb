<div class="h-full md:flex p-3 border border-gray-900 rounded-lg">
  <div class="md:flex-1 mr-3">
    <a href="<%= url_for link %>" class="hover:text-white mr-3"><%= title %></a>
    <p class="text-gray-600 text-sm"><%= description %></p>
  </div>
  <div class="mt-3 md:mt-0 md:ml-auto md:text-center">
    <%= render 'component/btn-sm', text: (t :use_tool), link: link %>
  </div>
</div>
